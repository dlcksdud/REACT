import React from 'react'

const Rsp = (props) => {

    return (
        <div>
        {/* <button onClick={() => rspState(props.name)}><p>{props.name}</p></button> */}
        <button><p>{props.name}</p></button>
        </div>
    )
}

export default Rsp
